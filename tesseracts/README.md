# Tesseract definitions

Every subfolder of this directory contains the build context of a Tesseract. The script `buildall.sh` at the root of this repository loops over all subdirectories and calls `tesseract build` one by one.
